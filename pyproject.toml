[project]
name = "autoenv"
version = "0.1.0"
description = "Autonomous research loop for building verifiers environments"
requires-python = ">=3.11"
dependencies = [
    "numpy",
    "openai",
    "verifiers>=0.1.9",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
