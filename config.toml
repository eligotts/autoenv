# AutoEnv configuration
# This file is FIXED — the agent should not modify it.

[eval]
# Target model — the model we intend to train with RL
# Aim for mean reward 0.2-0.7 against this model.
target_model = "qwen/qwen3-235b-a22b-instruct-2507"

# Strong model — used to verify tasks are solvable and well-designed.
# Should score high (>0.7). If the strong model can't solve the tasks,
# they're too hard or poorly designed.
strong_model = "openai/gpt-5.2"

# Fixed eval budget — same for every iteration so results are comparable
num_examples = 10
rollouts_per_example = 2

[eval.vf]
# Path to verifiers endpoints registry
endpoints_path = "./verifiers/configs/endpoints.toml"
# Provider for prime eval
provider = "prime"
# Max concurrent requests
max_concurrent = 16

# Qualitative feedback is generated by a Claude Code judge instance
# launched from evaluate.sh — no API config needed here.
